name: Frontend CI/CD (Docker Hub)

on:
  push:
    branches:
      - main
      - develop
      - product-*
      - production-*
    paths:
      - "src/**"
      - "public/**"
      - "index.html"
      - "vite.config.ts"
      - "package.json"
      - "package-lock.json"
      - "tsconfig*.json"
      - "nginx.conf"
      - "Dockerfile.fe"
      - ".github/workflows/fe-ci-cd.yml"
  workflow_dispatch:
    inputs:
      branch_name:
        description: Branch to build (defaults to the branch you dispatch from)
        required: false
        default: ""
      image_name:
        description: Docker image name (repo part after username)
        required: false
        default: "odocs-frontend"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  call-build-push:
    uses: ./.github/workflows/reusable-build-push.yml
    with:
      default_image_name: "ododocs-frontend"
      dockerfile: "./Dockerfile.fe"
      override_branch_name: ${{ inputs.branch_name }}
      override_image_name: ${{ inputs.image_name }}
    secrets: inherit
